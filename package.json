{
  "name": "simulador-sistema-flotante-l298n",
  "version": "0.3.0",
  "description": "Simulador de granja flotante eólica + firmware ESP32 (L298N + JGB-37).",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=spec \"tests/**/*.test.mjs\"",
    "serve": "python -m http.server 3000"
  }
}
